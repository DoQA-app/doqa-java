<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>app.doqa</groupId>
        <artifactId>doqa-java</artifactId>
        <version>0.1.6</version>
    </parent>

    <artifactId>doqa-testng</artifactId>
    <packaging>jar</packaging>

    <name>DoQA TestNG adapter</name>
    <description>
        TestNG adapter for DoQA: auto-registered listeners that report test results with steps,
        fixtures, parameters, attachments and links either directly to the DoQA API or as
        Allure-compatible result files, driven by annotations (@DoqaId, @DoqaTitle, ...), the Doqa
        runtime API and AspectJ @Step weaving.
    </description>

    <dependencies>
        <dependency>
            <groupId>app.doqa</groupId>
            <artifactId>doqa-java-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- provided: the host test project already has TestNG on the classpath. Compiled against
             the current release; the adapter avoids newer API so it also runs on TestNG 7.4. -->
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>${testng.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- The @Step aspect (aspectjrt) arrives via doqa-java-commons; the weaver is the
             host's responsibility (LTW via -javaagent or the aspectj-maven-plugin at build time). -->
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjweaver</artifactId>
            <version>${aspectj.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <!-- real argument names for invocation parameters and {param} placeholders
                         (hosts should enable the same, else names are arg0..argN). -->
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>initialize</phase>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>-javaagent:${org.aspectj:aspectjweaver:jar} --add-opens java.base/java.lang=ALL-UNNAMED</argLine>
                    <systemPropertyVariables>
                        <doqa.reporting>off</doqa.reporting>
                        <doqa.resultsDir>${project.build.directory}/results</doqa.resultsDir>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
